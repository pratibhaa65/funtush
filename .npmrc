# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
# Keep node_modules layout strict (no implicit hoisting of undeclared deps),
# but allow workspace-root tooling bins to resolve in package scripts.
node-linker=hoisted